import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    horizontalLine: {
        borderBottomWidth: 1,
        marginTop: 10,
        marginBottom: 10
    }
});

export default styles;