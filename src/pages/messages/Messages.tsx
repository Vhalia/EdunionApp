import { View } from "react-native";
import MainText from "../../modules/text/MainText";
import { ColorConstants } from "../../constants/ThemeConstants";

const Messages = () => {
    return(
        <MainText weight={'700'} fontSize={20} text="Messages page" />
    );
}

export default Messages;