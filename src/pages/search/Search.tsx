import { Text } from "react-native-svg";
import MainText from "../../modules/text/MainText";

const Search = () => {
    return(
        <MainText weight={'700'} fontSize={20} text="Search page" />
    );
}

export default Search